const c=window.LINKDESK_CONFIG;const routes={'LinkDesk-Windows-0.1.0.exe':'windows','LinkDesk-Mac-arm64-0.1.0.zip':'mac','LinkDesk-Mac-x64-0.1.0.zip':'mac-intel','LinkDesk-Android-0.1.0.apk':'android'};document.querySelectorAll('[data-asset]').forEach(a=>{a.href='/download/'+routes[a.dataset.asset];a.setAttribute('download',a.dataset.asset);});const link=document.querySelector('#ios-link');if(link)link.href=c.ios;
if('serviceWorker'in navigator)navigator.serviceWorker.getRegistrations().then(rs=>Promise.all(rs.map(r=>r.unregister())));if('caches'in window)caches.keys().then(ks=>Promise.all(ks.filter(k=>k.startsWith('linkdesk-')).map(k=>caches.delete(k))));
