const c=window.LINKDESK_CONFIG;document.querySelectorAll('[data-asset]').forEach(a=>a.href=c.downloads.replace('/tag/','/download/')+'/'+a.dataset.asset);const link=document.querySelector('#ios-link');if(link)link.href=c.ios;
if('serviceWorker'in navigator)navigator.serviceWorker.getRegistrations().then(rs=>Promise.all(rs.map(r=>r.unregister())));if('caches'in window)caches.keys().then(ks=>Promise.all(ks.filter(k=>k.startsWith('linkdesk-')).map(k=>caches.delete(k))));
