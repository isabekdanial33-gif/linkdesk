import test from 'node:test';import assert from 'node:assert/strict';import {inputBuffer} from '../web/input-buffer.mjs';
test('coalesces motion but preserves press/release order',()=>{let frame;const events=[];const push=inputBuffer(e=>events.push(e),f=>{frame=f;return 1;},()=>{});push({type:'pointer',action:'down'});for(let x=0;x<100;x++)push({type:'pointer',action:'move',x});push({type:'pointer',action:'up'});frame();assert.deepEqual(events,[{type:'pointer',action:'down'},{type:'pointer',action:'move',x:99},{type:'pointer',action:'up'}]);});
test('pending input cannot leak into a new session',()=>{let frame;const events=[];const push=inputBuffer(e=>events.push(e),f=>{frame=f;return 1;},()=>{});push({type:'pointer',action:'move',x:1});push.clear();frame();assert.equal(events.length,0);});
